{
  "tz_offset": {
    "type": "int",
    "description": "时区修正（小时）：MCP 返回 UTC 时间，修正到本地时区",
    "hint": "北京时间填 8。若推送/赛程显示的时间与本地相差 N 小时，填入对应偏移",
    "default": 8
  },
  "push_goal": {
    "type": "bool",
    "description": "进球时推送",
    "default": true
  },
  "push_start": {
    "type": "bool",
    "description": "开赛时推送",
    "default": true
  },
  "push_finish": {
    "type": "bool",
    "description": "完场时推送",
    "default": true
  },
  "daily_schedule": {
    "type": "bool",
    "description": "每天早上 8 点推送当日关注赛程",
    "default": true
  },
  "remind_minutes": {
    "type": "int",
    "description": "开赛前提醒分钟数（0 为关闭）",
    "hint": "默认 15：daily 发现关注比赛后，在开赛前 15 分钟自动创建一次性提醒任务",
    "default": 15
  }
}
